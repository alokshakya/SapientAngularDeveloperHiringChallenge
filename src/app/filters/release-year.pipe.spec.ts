import { ReleaseYearPipe } from './release-year.pipe';

describe('ReleaseYearPipe', () => {
  it('create an instance', () => {
    const pipe = new ReleaseYearPipe();
    expect(pipe).toBeTruthy();
  });
});
