import { EditorsChoicePipe } from './editors-choice.pipe';

describe('EditorsChoicePipe', () => {
  it('create an instance', () => {
    const pipe = new EditorsChoicePipe();
    expect(pipe).toBeTruthy();
  });
});
